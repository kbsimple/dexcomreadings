---
phase: "03"
plan: "01"
type: testing
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, INFRA-02]
---

# Phase 3 Plan 01: Testing & Documentation

**Objective:** Test suite validates production behavior and users have complete setup documentation.

## Context

@03-CONTEXT.md

## Tasks

### Task 1: Fix test mocks
**Type:** auto
**Action:** Update `dexcom_readings_test.py` to mock `logging` module instead of `builtins.print`
**Done when:** All tests pass with corrected mocks

### Task 2: Add Nightscout upload tests
**Type:** auto
**Action:** Add `test_upload_to_nightscout_success()` and `test_upload_to_nightscout_failure()` tests
**Done when:** Tests cover success case, missing credentials, and HTTP error cases

### Task 3: Verify exit consistency
**Type:** auto
**Action:** Grep for bare `exit()` calls, verify only `sys.exit()` is used
**Done when:** No bare `exit()` calls found in production code

### Task 4: Create README.md
**Type:** auto
**Action:** Create documentation with installation, all environment variables, usage examples, license
**Done when:** README.md covers DEXCOM_USERNAME, DEXCOM_PASSWORD, DEXCOM_REGION, NIGHTSCOUT_URL, NIGHTSCOUT_API_SECRET, POLLING_INTERVAL_SECONDS

## Verification

- `python -m pytest dexcom_readings_test.py -v` passes
- `grep -n "exit(" dexcom_readings.py` shows only `sys.exit(1)`
- README.md exists with all required sections

## Success Criteria

1. All existing tests pass with corrected mocks using `logging` module
2. `upload_to_nightscout()` function has test coverage
3. Codebase uses `sys.exit()` consistently (no bare `exit()` calls)
4. `README.md` documents installation, configuration, and usage