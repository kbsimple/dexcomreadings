---
phase: 01-style-guide-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [dexcom_readings.py]
autonomous: true
requirements: [STYLE-02, STYLE-03]
user_setup: []

must_haves:
  truths:
    - "All public functions have docstrings with Args, Returns, and Raises sections"
    - "All function signatures display type hints when inspected via help() or IDE"
    - "Type hints use modern syntax (X | None instead of Optional[X])"
  artifacts:
    - path: "dexcom_readings.py"
      provides: "Documented, typed module"
      pattern: "def \\w+\\([^)]*\\)\\s*->"
  key_links:
    - from: "function signature"
      to: "type checker"
      via: "type hints"
      pattern: "-> "
---

<objective>
Add comprehensive docstrings and type hints to all public functions.

Purpose: Enable callers to understand function contracts without reading implementation, and enable type checking tools.
Output: dexcom_readings.py with Google-style docstrings and type hints on all public functions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-style-guide-infrastructure/01-02-SUMMARY.md
@.planning/research/STYLE_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add comprehensive docstrings to all public functions</name>
  <files>dexcom_readings.py</files>
  <read_first>
    <file>dexcom_readings.py</file>
    <file lines="9-27">.planning/research/STYLE_GUIDE.md</file>
  </read_first>
  <action>Add Google-style docstrings to all public functions in `dexcom_readings.py`. Each docstring must include:
- One-line summary
- Longer description (if needed)
- `Args:` section listing each parameter with type and description
- `Returns:` section describing return value and type
- `Raises:` section if exceptions can be raised

**Functions to document:**

1. `initialize_dexcom_client()`:
```python
def initialize_dexcom_client():
    """Initializes and authenticates a Dexcom Share API client.

    Reads credentials from environment variables (DEXCOM_USERNAME,
    DEXCOM_PASSWORD, DEXCOM_REGION) and creates an authenticated
    Dexcom client for fetching glucose readings.

    Args:
        None

    Returns:
        Dexcom | None: An authenticated Dexcom client instance, or None
            if credentials are missing or authentication fails.

    Raises:
        No exceptions raised; errors are logged and None is returned.
    """
```

2. `get_latest_glucose_reading(dexcom_client)`:
```python
def get_latest_glucose_reading(dexcom_client):
    """Fetches the most recent glucose reading from the Dexcom API.

    Args:
        dexcom_client: An authenticated Dexcom client instance from
            pydexcom library.

    Returns:
        GlucoseReading | None: The latest glucose reading object containing
            value, datetime, and trend information, or None if the fetch
            fails or client is invalid.

    Raises:
        No exceptions raised; errors are logged and None is returned.
    """
```

3. `write_to_csv(data_row)`:
```python
def write_to_csv(data_row):
    """Appends a glucose reading data row to the CSV log file.

    Creates the file with headers if it doesn't exist, otherwise appends
    the data row to the existing file.

    Args:
        data_row: A list of values to write as a CSV row. Expected format:
            [check_timestamp_utc, new_reading_received, glucose_value_mgdl,
             glucose_timestamp_utc, trend_description, trend_arrow]

    Returns:
        None

    Raises:
        No exceptions raised; file I/O errors are not caught here.
    """
```

4. `upload_to_nightscout(value, timestamp_utc, trend_arrow)`:
```python
def upload_to_nightscout(value, timestamp_utc, trend_arrow):
    """Uploads a glucose reading to Nightscout via REST API.

    Sends a sensor glucose value (SGV) entry to the Nightscout API.
    Requires NIGHTSCOUT_URL and NIGHTSCOUT_API_SECRET environment
    variables to be set. If either is missing, the function returns
    early without uploading.

    Args:
        value: The glucose value in mg/dL as an integer.
        timestamp_utc: A datetime object representing the reading timestamp
            in UTC.
        trend_arrow: A string representing the trend direction arrow
            (e.g., "→", "↑", "↓↓").

    Returns:
        None

    Raises:
        No exceptions raised; HTTP and network errors are logged and the
            function returns silently.
    """
```

5. `main()`:
```python
def main():
    """Main polling loop for Dexcom glucose readings.

    Continuously polls the Dexcom Share API for new glucose readings,
    logs data to CSV, and uploads to Nightscout when configured.
    Runs indefinitely until interrupted.

    Args:
        None

    Returns:
        None

    Raises:
        SystemExit: If Dexcom client initialization fails (exit code 1).
    """
```

**Note:** Use `GlucoseReading | None` syntax (modern union type) in Returns sections, not `Optional[GlucoseReading]`.</action>
  <verify>
    <automated>python3 -c "
import ast
import re

with open('dexcom_readings.py') as f:
    tree = ast.parse(f.read())

functions = ['initialize_dexcom_client', 'get_latest_glucose_reading', 'write_to_csv', 'upload_to_nightscout', 'main']
for node in ast.walk(tree):
    if isinstance(node, ast.FunctionDef):
        if node.name in functions:
            docstring = ast.get_docstring(node)
            assert docstring is not None, f'{node.name} missing docstring'
            assert 'Args:' in docstring or node.name == 'main', f'{node.name} missing Args section'
            assert 'Returns:' in docstring, f'{node.name} missing Returns section'
            functions.remove(node.name)

assert len(functions) == 0, f'Missing docstrings for: {functions}'
print('All public functions have Google-style docstrings')
"</automated>
  </verify>
  <acceptance_criteria>
    - `initialize_dexcom_client` has docstring with Args and Returns sections
    - `get_latest_glucose_reading` has docstring with Args and Returns sections
    - `write_to_csv` has docstring with Args and Returns sections
    - `upload_to_nightscout` has docstring with Args and Returns sections
    - `main` has docstring with Returns section (no Args since no parameters)
    - All docstrings use Google-style format (not NumPy or Sphinx style)
    - No `Optional[...]` syntax used; use `X | None` format
  </acceptance_criteria>
  <done>All public functions in dexcom_readings.py have Google-style docstrings with Args/Returns/Raises sections.</done>
</task>

<task type="auto">
  <name>Task 2: Add type hints to all function signatures</name>
  <files>dexcom_readings.py</files>
  <read_first>
    <file>dexcom_readings.py</file>
    <file lines="29-39">.planning/research/STYLE_GUIDE.md</file>
  </read_first>
  <action>Add type hints to all function signatures in `dexcom_readings.py`. Use modern Python syntax (`X | None` instead of `Optional[X]`).

**Type hints to add:**

1. Add necessary imports at the top (after existing imports, before constants):
```python
from typing import Any
```
Note: `GlucoseReading` comes from pydexcom and should be imported from the library if available, or use `Any` if the type isn't directly importable.

2. `initialize_dexcom_client()`:
```python
def initialize_dexcom_client() -> Any | None:
```
(Using `Any` for Dexcom client since it's from a third-party library)

3. `get_latest_glucose_reading(dexcom_client)`:
```python
def get_latest_glucose_reading(dexcom_client: Any) -> Any | None:
```
(Returns GlucoseReading or None, using Any for the third-party types)

4. `write_to_csv(data_row)`:
```python
def write_to_csv(data_row: list) -> None:
```

5. `upload_to_nightscout(value, timestamp_utc, trend_arrow)`:
```python
def upload_to_nightscout(value: int, timestamp_utc: datetime.datetime, trend_arrow: str) -> None:
```
(Note: `datetime` is already imported from the `datetime` module via `import datetime`, so use `datetime.datetime`)

6. `main()`:
```python
def main() -> None:
```

**Important:**
- Add `from typing import Any` after the other stdlib imports
- Use `Any | None` for return types where the exact type is from a third-party library
- Use `-> None` for functions that don't return a meaningful value
- Use `datetime.datetime` for the datetime type hint (since `import datetime` imports the module, not the class)
- Do NOT annotate `self` or `cls` (not applicable here - no classes)
- Do NOT annotate `__init__` return value (not applicable here)</action>
  <verify>
    <automated>python3 -c "
import ast
import re

with open('dexcom_readings.py') as f:
    content = f.read()
    tree = ast.parse(content)

# Check for typing import
assert 'from typing import Any' in content or 'import typing' in content, 'Missing typing import'

functions = {
    'initialize_dexcom_client': '-> ',
    'get_latest_glucose_reading': '-> ',
    'write_to_csv': '-> ',
    'upload_to_nightscout': '-> ',
    'main': '-> '
}

for func_name, expected in functions.items():
    pattern = rf'def {func_name}\\([^)]*\\)\\s*->'
    assert re.search(pattern, content), f'{func_name} missing return type hint'

# Verify None type hint is used where appropriate
assert '-> None' in content, 'Missing -> None type hint'

# Verify modern union syntax
if 'Optional[' in content:
    # Check if it's in a comment or string
    lines_with_optional = [l for l in content.split('\\n') if 'Optional[' in l and not l.strip().startswith('#') and '\"\"\"' not in l]
    assert len(lines_with_optional) == 0, 'Found Optional[...] syntax; use X | None instead'

print('All functions have type hints with modern syntax')
"</automated>
  </verify>
  <acceptance_criteria>
    - `from typing import Any` is imported after stdlib imports
    - `initialize_dexcom_client` has return type hint `-> Any | None`
    - `get_latest_glucose_reading` has parameter type `dexcom_client: Any` and return type `-> Any | None`
    - `write_to_csv` has parameter type `data_row: list` and return type `-> None`
    - `upload_to_nightscout` has parameter types `value: int`, `timestamp_utc: datetime.datetime`, `trend_arrow: str` and return type `-> None`
    - `main` has return type `-> None`
    - No use of `Optional[...]` syntax (use `X | None` instead)
    - File parses correctly with type hints
  </acceptance_criteria>
  <done>All function signatures in dexcom_readings.py have type hints using modern Python syntax.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type system | Type hints provide compile-time verification of function contracts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new threats from documentation changes |

No security impact - this is documentation and type annotation only.
</threat_model>

<verification>
- All functions have return type hints (`->`)
- All parameters have type annotations
- `from typing import Any` is imported
- No `Optional[...]` syntax used
- `python3 -m py_compile dexcom_readings.py` succeeds
- `python3 -c "import dexcom_readings; help(dexcom_readings.initialize_dexcom_client)"` shows function signature with type hints
</verification>

<success_criteria>
1. `initialize_dexcom_client()` signature shows `-> Any | None`
2. `get_latest_glucose_reading()` signature shows typed parameters and return
3. `write_to_csv()` signature shows `-> None`
4. `upload_to_nightscout()` signature shows typed parameters and `-> None`
5. `main()` signature shows `-> None`
6. All docstrings have Args and Returns sections
</success_criteria>

<output>
After completion, create `.planning/phases/01-style-guide-infrastructure/01-03-SUMMARY.md`
</output>