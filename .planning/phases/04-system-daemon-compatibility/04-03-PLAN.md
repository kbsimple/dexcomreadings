---
phase: 04-system-daemon-compatibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
files_created: [service/dexcom-readings.service, service/com.dexcom.readings.plist]
autonomous: true
requirements: [DAEMON-05]
user_setup: []

must_haves:
  truths:
    - "User can deploy as systemd service on Linux"
    - "User can deploy as launchd service on macOS"
    - "Service templates use absolute paths"
    - "README documents daemon deployment steps"
    - "Templates include environment variable configuration"
  artifacts:
    - path: "service/dexcom-readings.service"
      provides: "systemd unit file template"
      contains: ["ExecStart", "EnvironmentFile", "Restart=on-failure"]
    - path: "service/com.dexcom.readings.plist"
      provides: "launchd plist template"
      contains: ["ProgramArguments", "EnvironmentVariables", "RunAtLoad"]
    - path: "README.md"
      provides: "Daemon deployment documentation"
      contains: ["## Daemon Deployment", "systemd", "launchd"]
  key_links:
    - from: "service templates"
      to: "README.md documentation"
      via: "deployment instructions reference template files"
      pattern: "dexcom-readings.service|com.dexcom.readings.plist"
---

<objective>
Create systemd and launchd service templates with documentation for daemon deployment.

Purpose: Enable users to deploy dexcom_readings as a system service on Linux (systemd) and macOS (launchd).

Output: service/dexcom-readings.service, service/com.dexcom.readings.plist, updated README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-system-daemon-compatibility/04-RESEARCH.md

<interfaces>
<!-- Service template patterns from research -->

From 04-RESEARCH.md (systemd template):
```ini
[Unit]
Description=Dexcom CGM Readings Collector
Documentation=https://github.com/user/dexcom-readings
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=dexcom
Group=dexcom
WorkingDirectory=/opt/dexcom-readings
ExecStart=/opt/dexcom-readings/venv/bin/python /opt/dexcom-readings/dexcom_readings.py
Restart=on-failure
RestartSec=5s

# Environment variables
EnvironmentFile=/etc/default/dexcom-readings

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=dexcom-readings

# Security sandboxing
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/var/lib/dexcom-readings

[Install]
WantedBy=multi-user.target
```

From 04-RESEARCH.md (launchd template):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.dexcom.readings</string>
    <key>ProgramArguments</key>
    <array>
        <string>/opt/dexcom-readings/venv/bin/python</string>
        <string>/opt/dexcom-readings/dexcom_readings.py</string>
    </array>
    <key>WorkingDirectory</key>
    <string>/opt/dexcom-readings</string>
    <key>EnvironmentVariables</key>
    <dict>
        <key>DEXCOM_USERNAME</key>
        <string>YOUR_USERNAME_HERE</string>
        <key>DEXCOM_PASSWORD</key>
        <string>YOUR_PASSWORD_HERE</string>
    </dict>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
    <key>StandardOutPath</key>
    <string>/var/log/dexcom-readings.log</string>
    <key>StandardErrorPath</key>
    <string>/var/log/dexcom-readings.log</string>
</dict>
</plist>
```

From README.md (existing structure):
- Installation section at line 9
- Configuration section at line 29
- Usage section at line 71
- Troubleshooting section at line 122
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create systemd service template</name>
  <files>service/dexcom-readings.service</files>
  <read_first>
    - .planning/phases/04-system-daemon-compatibility/04-RESEARCH.md (Pattern 5: systemd Service Unit Template)
  </read_first>
  <action>
Create the service directory and systemd unit file template.

1. Create directory: `service/`

2. Create file: `service/dexcom-readings.service` with content:

```ini
# Dexcom Readings - systemd Service Unit Template
#
# Installation (Linux with systemd):
# 1. Copy this file to /etc/systemd/system/dexcom-readings.service
# 2. Create environment file: /etc/default/dexcom-readings
# 3. Create user: sudo useradd -r -s /bin/false dexcom
# 4. Create directories:
#    sudo mkdir -p /opt/dexcom-readings
#    sudo mkdir -p /var/lib/dexcom-readings
#    sudo chown dexcom:dexcom /var/lib/dexcom-readings
# 5. Copy application files to /opt/dexcom-readings/
# 6. Create virtual environment and install dependencies:
#    cd /opt/dexcom-readings
#    python3 -m venv venv
#    ./venv/bin/pip install -r requirements.txt
# 7. Enable and start:
#    sudo systemctl daemon-reload
#    sudo systemctl enable dexcom-readings
#    sudo systemctl start dexcom-readings
#
# Environment file format (/etc/default/dexcom-readings):
# DEXCOM_USERNAME="your_username"
# DEXCOM_PASSWORD="your_password"
# DEXCOM_REGION="us"
# NIGHTSCOUT_URL="https://your-nightscout.example.com"
# NIGHTSCOUT_API_SECRET="your_secret"
# POLLING_INTERVAL_SECONDS="60"
# DEXCOM_LOG_DESTINATION="file"
# DEXCOM_LOG_FILE="/var/log/dexcom-readings/dexcom-readings.log"
# DEXCOM_CSV_PATH="/var/lib/dexcom-readings/readings.csv"
# DEXCOM_PID_FILE="/var/run/dexcom-readings/dexcom-readings.pid"

[Unit]
Description=Dexcom CGM Readings Collector
Documentation=https://github.com/user/dexcom-readings
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=dexcom
Group=dexcom
WorkingDirectory=/opt/dexcom-readings
ExecStart=/opt/dexcom-readings/venv/bin/python /opt/dexcom-readings/dexcom_readings.py
Restart=on-failure
RestartSec=5s

# Environment variables
EnvironmentFile=/etc/default/dexcom-readings

# Logging to systemd journal
StandardOutput=journal
StandardError=journal
SyslogIdentifier=dexcom-readings

# Security sandboxing
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/var/lib/dexcom-readings /var/log/dexcom-readings /var/run/dexcom-readings

[Install]
WantedBy=multi-user.target
```

Note: The paths use `/opt/dexcom-readings` as the installation directory, `/var/lib/dexcom-readings` for data, and `/var/log/dexcom-readings` for logs. These are common Linux conventions but users should adjust to their system.
  </action>
  <acceptance_criteria>
    - service/dexcom-readings.service file exists
    - File contains `[Unit]` section with `Description=Dexcom CGM Readings Collector`
    - File contains `[Service]` section with `Type=simple`
    - File contains `ExecStart=` with absolute Python path
    - File contains `EnvironmentFile=/etc/default/dexcom-readings`
    - File contains `Restart=on-failure`
    - File contains `[Install]` section with `WantedBy=multi-user.target`
    - File contains installation instructions as comments
  </acceptance_criteria>
  <verify>
    <automated>grep -E "^\[Unit\]|\[Service\]|\[Install\]|ExecStart=|EnvironmentFile=|Type=simple" service/dexcom-readings.service</automated>
  </verify>
  <done>
systemd service unit template created with complete installation instructions, security sandboxing, and environment file support.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create launchd plist template</name>
  <files>service/com.dexcom.readings.plist</files>
  <read_first>
    - .planning/phases/04-system-daemon-compatibility/04-RESEARCH.md (Pattern 6: launchd plist Template)
  </read_first>
  <action>
Create launchd plist template for macOS deployment.

Create file: `service/com.dexcom.readings.plist` with content:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
    Dexcom Readings - launchd Service Template for macOS

    Installation:
    1. Copy this file to ~/Library/LaunchAgents/ (user) or /Library/LaunchDaemons/ (system)
       For user-level: ~/Library/LaunchAgents/com.dexcom.readings.plist
       For system-level: /Library/LaunchDaemons/com.dexcom.readings.plist

    2. Edit this file to set your credentials and paths:
       - Replace /opt/dexcom-readings with your installation path
       - Replace YOUR_USERNAME_HERE with your Dexcom username
       - Replace YOUR_PASSWORD_HERE with your Dexcom password
       - Update other environment variables as needed

    3. Create installation directory:
       mkdir -p /opt/dexcom-readings
       cd /opt/dexcom-readings
       python3 -m venv venv
       ./venv/bin/pip install -r requirements.txt
       # Copy dexcom_readings.py to this directory

    4. Load the service:
       launchctl load ~/Library/LaunchAgents/com.dexcom.readings.plist

    5. Check status:
       launchctl list | grep dexcom

    6. View logs:
       tail -f /var/log/dexcom-readings.log

    To unload:
       launchctl unload ~/Library/LaunchAgents/com.dexcom.readings.plist

    IMPORTANT: launchd does not expand shell variables. Use absolute paths.
    -->
    <key>Label</key>
    <string>com.dexcom.readings</string>

    <key>ProgramArguments</key>
    <array>
        <!-- UPDATE THIS: Path to Python in your virtual environment -->
        <string>/opt/dexcom-readings/venv/bin/python</string>
        <!-- UPDATE THIS: Path to the script -->
        <string>/opt/dexcom-readings/dexcom_readings.py</string>
    </array>

    <key>WorkingDirectory</key>
    <!-- UPDATE THIS: Directory containing the script -->
    <string>/opt/dexcom-readings</string>

    <key>EnvironmentVariables</key>
    <dict>
        <!-- Required: Your Dexcom credentials -->
        <key>DEXCOM_USERNAME</key>
        <string>YOUR_USERNAME_HERE</string>
        <key>DEXCOM_PASSWORD</key>
        <string>YOUR_PASSWORD_HERE</string>

        <!-- Optional: Region (us, ous, jp) -->
        <key>DEXCOM_REGION</key>
        <string>us</string>

        <!-- Optional: Nightscout configuration -->
        <key>NIGHTSCOUT_URL</key>
        <string></string>
        <key>NIGHTSCOUT_API_SECRET</key>
        <string></string>

        <!-- Optional: Polling interval in seconds -->
        <key>POLLING_INTERVAL_SECONDS</key>
        <string>60</string>

        <!-- Optional: Logging configuration -->
        <key>DEXCOM_LOG_DESTINATION</key>
        <string>file</string>
        <key>DEXCOM_LOG_FILE</key>
        <string>/var/log/dexcom-readings.log</string>

        <!-- Optional: Data paths -->
        <key>DEXCOM_CSV_PATH</key>
        <string>/var/lib/dexcom-readings/readings.csv</string>
        <key>DEXCOM_PID_FILE</key>
        <string>/var/run/dexcom-readings.pid</string>
    </dict>

    <!-- Start when loaded -->
    <key>RunAtLoad</key>
    <true/>

    <!-- Restart on failure -->
    <key>KeepAlive</key>
    <dict>
        <key>SuccessfulExit</key>
        <false/>
    </dict>

    <!-- Log output -->
    <key>StandardOutPath</key>
    <string>/var/log/dexcom-readings.log</string>
    <key>StandardErrorPath</key>
    <string>/var/log/dexcom-readings.log</string>

    <!-- Process type: background -->
    <key>ProcessType</key>
    <string>Background</string>
</dict>
</plist>
```

Note: For macOS, launchd uses XML property lists. The template includes comments explaining each setting and how to customize.
  </action>
  <acceptance_criteria>
    - service/com.dexcom.readings.plist file exists
    - File contains valid XML with `<!DOCTYPE plist` declaration
    - File contains `<key>Label</key>` with `<string>com.dexcom.readings</string>`
    - File contains `<key>ProgramArguments</key>` array with Python path and script path
    - File contains `<key>EnvironmentVariables</key>` dict with DEXCOM_USERNAME, DEXCOM_PASSWORD
    - File contains `<key>RunAtLoad</key><true/>`
    - File contains `<key>KeepAlive</key>` dict
    - File contains installation instructions as XML comments
  </acceptance_criteria>
  <verify>
    <automated>plutil -lint service/com.dexcom.readings.plist</automated>
  </verify>
  <done>
launchd plist template created with complete installation instructions and all configurable environment variables.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update README with daemon deployment documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md (existing structure and content)
    - service/dexcom-readings.service (for systemd instructions)
    - service/com.dexcom.readings.plist (for launchd instructions)
  </read_first>
  <action>
Add a Daemon Deployment section to README.md after the Troubleshooting section.

1. Find the Troubleshooting section (around line 122) and add the new section after it, before the License section:

Add after the Troubleshooting section (before `## License`):

```markdown
## Daemon Deployment

The service can be deployed as a system daemon for continuous, unattended operation.

### Environment Variables for Daemon Mode

| Variable | Description | Default |
|----------|-------------|---------|
| `DEXCOM_LOG_DESTINATION` | Logging destination: `console`, `file`, or `syslog` | `console` |
| `DEXCOM_LOG_LEVEL` | Log level: `DEBUG`, `INFO`, `WARNING`, `ERROR` | `INFO` |
| `DEXCOM_LOG_FILE` | Path to log file (when `DEXCOM_LOG_DESTINATION=file`) | `~/.local/state/dexcom-readings/dexcom-readings.log` |
| `DEXCOM_CSV_PATH` | Path to CSV data file | `~/.local/share/dexcom-readings/readings.csv` |
| `DEXCOM_PID_FILE` | Path to PID file for single-instance locking | `~/.local/state/dexcom-readings/dexcom-readings.pid` |

### Linux (systemd)

1. Copy the service template:
   ```bash
   sudo cp service/dexcom-readings.service /etc/systemd/system/
   ```

2. Create the environment file `/etc/default/dexcom-readings`:
   ```bash
   DEXCOM_USERNAME="your_username"
   DEXCOM_PASSWORD="your_password"
   DEXCOM_REGION="us"
   NIGHTSCOUT_URL="https://your-nightscout.example.com"
   NIGHTSCOUT_API_SECRET="your_secret"
   DEXCOM_LOG_DESTINATION="file"
   DEXCOM_LOG_FILE="/var/log/dexcom-readings/dexcom-readings.log"
   ```

3. Create user and directories:
   ```bash
   sudo useradd -r -s /bin/false dexcom
   sudo mkdir -p /opt/dexcom-readings /var/lib/dexcom-readings /var/log/dexcom-readings
   sudo chown dexcom:dexcom /var/lib/dexcom-readings /var/log/dexcom-readings
   ```

4. Install the application:
   ```bash
   # Copy files to /opt/dexcom-readings/
   cd /opt/dexcom-readings
   python3 -m venv venv
   ./venv/bin/pip install -r requirements.txt
   ```

5. Enable and start the service:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable dexcom-readings
   sudo systemctl start dexcom-readings
   ```

6. View logs:
   ```bash
   journalctl -u dexcom-readings -f
   ```

### macOS (launchd)

1. Copy the plist template:
   ```bash
   cp service/com.dexcom.readings.plist ~/Library/LaunchAgents/
   ```

2. Edit the plist to set your credentials and paths (see comments in the file).

3. Create the installation directory:
   ```bash
   mkdir -p /opt/dexcom-readings
   cd /opt/dexcom-readings
   python3 -m venv venv
   ./venv/bin/pip install -r requirements.txt
   # Copy dexcom_readings.py to this directory
   ```

4. Load the service:
   ```bash
   launchctl load ~/Library/LaunchAgents/com.dexcom.readings.plist
   ```

5. Check status:
   ```bash
   launchctl list | grep dexcom
   ```

### Log Rotation

For file-based logging, use external log rotation tools:

**Linux (logrotate):**
Create `/etc/logrotate.d/dexcom-readings`:
```
/var/log/dexcom-readings/*.log {
    daily
    rotate 7
    compress
    missingok
    notifempty
    postrotate
        systemctl kill -s HUP dexcom-readings.service
    endscript
}
```

The `SIGHUP` signal triggers the daemon to reopen the log file after rotation.
```

2. The section should be inserted between the Troubleshooting section and the License section.

3. Make sure the table formatting aligns with existing tables in the README.
  </action>
  <acceptance_criteria>
    - README.md contains `## Daemon Deployment` section
    - README.md contains `### Environment Variables for Daemon Mode` subsection
    - README.md contains `### Linux (systemd)` subsection with installation steps
    - README.md contains `### macOS (launchd)` subsection with installation steps
    - README.md contains `### Log Rotation` subsection with logrotate example
    - README.md documents DEXCOM_LOG_DESTINATION, DEXCOM_LOG_LEVEL, DEXCOM_LOG_FILE, DEXCOM_CSV_PATH, DEXCOM_PID_FILE
    - README.md references service/dexcom-readings.service and service/com.dexcom.readings.plist
    - README.md contains `systemctl kill -s HUP` for log rotation
  </acceptance_criteria>
  <verify>
    <automated>grep -E "## Daemon Deployment|### Linux \(systemd\)|### macOS \(launchd\)|### Log Rotation|DEXCOM_LOG_DESTINATION|systemctl kill -s HUP" README.md</automated>
  </verify>
  <done>
README.md updated with comprehensive daemon deployment documentation covering systemd, launchd, and log rotation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service configuration files | User-edited files contain credentials |
| Filesystem paths | Templates reference system directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | Credentials in plist | mitigate | Document that plist should be readable only by owner (chmod 600); recommend system keychain for production |
| T-04-08 | Elevation | systemd service | mitigate | Service runs as dedicated user (not root), NoNewPrivileges=true in unit file |
| T-04-09 | Tampering | Environment file | mitigate | Document /etc/default/dexcom-readings should be mode 600, owned by root |
</threat_model>

<verification>
- service/dexcom-readings.service exists with valid systemd unit syntax
- service/com.dexcom.readings.plist exists with valid plist XML (plutil -lint passes)
- README.md contains Daemon Deployment section
</verification>

<success_criteria>
- Service file templates for systemd and launchd are provided and documented (DAEMON-05)
- Templates use absolute paths and include installation instructions
- README.md documents daemon deployment for both Linux and macOS
- Log rotation configuration is documented
</success_criteria>

<output>
After completion, create `.planning/phases/04-system-daemon-compatibility/04-03-SUMMARY.md`
</output>