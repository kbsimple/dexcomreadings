---
phase: quick-260424-u1l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dexcom_readings_test.py
autonomous: true
requirements:
  - STYLE-COMPLIANCE-01
must_haves:
  truths:
    - "dexcom_readings_test.py has no syntax errors"
    - "All 20 tests pass with python -m unittest"
    - "No unused imports remain"
    - "Two blank lines separate top-level class definitions"
    - "with-continuation lines align to 14-space indent (opening paren position)"
    - "No redundant patch.dict(os.environ) wrapping module-level patches"
  artifacts:
    - path: "dexcom_readings_test.py"
      provides: "Google Python Style Guide compliant test file"
  key_links:
    - from: "dexcom_readings_test.py"
      to: "python -m unittest"
      via: "All 20 tests pass after edits"
      pattern: "Ran 20 tests.*OK"
---

<objective>
Fix five Google Python Style Guide violations in dexcom_readings_test.py: a syntax
error from a redundant outer `with patch.dict` block, messy nesting in the missing-password
test, a missing blank line between top-level class definitions, an unused `call` import,
and inconsistent `with`-continuation indentation (17 spaces instead of 14).

Purpose: Keep the test file clean, parseable, and compliant with the project's style conventions.
Output: dexcom_readings_test.py with all issues corrected and all 20 tests still passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all five style violations in dexcom_readings_test.py</name>
  <files>dexcom_readings_test.py</files>
  <action>
Apply ALL of the following edits to dexcom_readings_test.py in a single pass:

**Fix 1 — Remove unused `call` import (line 11):**
Change:
  from unittest.mock import call, MagicMock, mock_open, patch
To:
  from unittest.mock import MagicMock, mock_open, patch

**Fix 2 — Add missing blank line between top-level classes (after line 32):**
After the closing of MockGlucoseReading (the `return NotImplemented` line), there is
currently only 1 blank line before `class TestDexcomReadings`. Insert a second blank
line so there are exactly 2 blank lines between the two top-level class definitions.

**Fix 3 — Fix `with`-continuation indentation at lines 56-58:**
The parenthesised `with` block opening is at column 8 (8 spaces before `with`),
so the opening `(` sits at column 13. Continuation lines must align under it at 14 spaces.
Current (17 spaces before `patch`):
    with (patch('dexcom_readings.DEXCOM_USERNAME', 'testuser'),
                 patch('dexcom_readings.DEXCOM_PASSWORD', 'testpassword'),
                 patch('dexcom_readings.DEXCOM_REGION', 'us')):
Change to (14 spaces before each continuation `patch`):
    with (patch('dexcom_readings.DEXCOM_USERNAME', 'testuser'),
              patch('dexcom_readings.DEXCOM_PASSWORD', 'testpassword'),
              patch('dexcom_readings.DEXCOM_REGION', 'us')):

**Fix 4 — Remove redundant outer `with patch.dict` and fix syntax error in
test_initialize_dexcom_client_missing_username (lines 86-89):**
Current broken code (two `with` statements at 8-space indent, the first is never
closed before the second begins — this is a syntax error):
    with patch.dict(os.environ, {"DEXCOM_PASSWORD": "testpassword"}, clear=True):
        with (patch('dexcom_readings.DEXCOM_USERNAME', None),
                 patch('dexcom_readings.DEXCOM_PASSWORD', "testpassword")):
            client = dexcom_readings.initialize_dexcom_client()
Replace with (outer `with patch.dict` removed entirely; inner `with` kept, continuation
aligned to 14 spaces):
    with (patch('dexcom_readings.DEXCOM_USERNAME', None),
              patch('dexcom_readings.DEXCOM_PASSWORD', "testpassword")):
        client = dexcom_readings.initialize_dexcom_client()

**Fix 5 — Remove redundant outer `with patch.dict` and fix nesting in
test_initialize_dexcom_client_missing_password (lines 99-102):**
Current messy code (outer `with` at 8 spaces, inner `with` at 13 spaces — non-standard):
    with patch.dict(os.environ, {"DEXCOM_USERNAME": "testuser"}, clear=True):
         with (patch('dexcom_readings.DEXCOM_USERNAME', "testuser"),
                  patch('dexcom_readings.DEXCOM_PASSWORD', None)):
            client = dexcom_readings.initialize_dexcom_client()
Replace with (outer `with patch.dict` removed; inner `with` at 8-space indent with
14-space continuation alignment):
    with (patch('dexcom_readings.DEXCOM_USERNAME', "testuser"),
              patch('dexcom_readings.DEXCOM_PASSWORD', None)):
        client = dexcom_readings.initialize_dexcom_client()

**Fix 6 — Fix `with`-continuation indentation at lines 114-116
(test_initialize_dexcom_client_api_error):**
Same 17-space problem as Fix 3. Change:
    with (patch('dexcom_readings.DEXCOM_USERNAME', 'testuser'),
                 patch('dexcom_readings.DEXCOM_PASSWORD', 'testpassword'),
                 patch('dexcom_readings.DEXCOM_REGION', 'us')):
To:
    with (patch('dexcom_readings.DEXCOM_USERNAME', 'testuser'),
              patch('dexcom_readings.DEXCOM_PASSWORD', 'testpassword'),
              patch('dexcom_readings.DEXCOM_REGION', 'us')):

**Fix 7 — Fix `with`-continuation indentation at lines 201-202
(test_upload_to_nightscout_missing_url):**
Change:
    with (patch('dexcom_readings.NIGHTSCOUT_URL', None),
                 patch('dexcom_readings.NIGHTSCOUT_API_SECRET', "secret")):
To:
    with (patch('dexcom_readings.NIGHTSCOUT_URL', None),
              patch('dexcom_readings.NIGHTSCOUT_API_SECRET', "secret")):

**Fix 8 — Fix `with`-continuation indentation at lines 209-210
(test_upload_to_nightscout_missing_secret):**
Change:
    with (patch('dexcom_readings.NIGHTSCOUT_URL', "https://example.com"),
                 patch('dexcom_readings.NIGHTSCOUT_API_SECRET', None)):
To:
    with (patch('dexcom_readings.NIGHTSCOUT_URL', "https://example.com"),
              patch('dexcom_readings.NIGHTSCOUT_API_SECRET', None)):

Do not change any other lines. Do not reformat docstrings or any test logic.
  </action>
  <verify>
    <automated>cd /home/kbsimple/claude-projects/dexcomreadings && python -m unittest dexcom_readings_test.py 2>&1 | tail -5</automated>
  </verify>
  <done>
    `python -m unittest dexcom_readings_test.py` reports "Ran 20 tests" with "OK" and no
    errors or failures. The file parses without SyntaxError. `call` no longer appears in
    the import line. Two blank lines separate MockGlucoseReading from TestDexcomReadings.
    All `with`-continuation lines use 14-space indentation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → python interpreter | File is parsed locally; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-u1l-01 | Tampering | dexcom_readings_test.py | accept | Style-only edits; no logic changes; all 20 tests must pass as automated gate |
</threat_model>

<verification>
Run: `python -m unittest dexcom_readings_test.py`
Expected: `Ran 20 tests in ...s` followed by `OK`
</verification>

<success_criteria>
- All 20 tests pass with no errors or failures
- No SyntaxError when the file is parsed
- `from unittest.mock import MagicMock, mock_open, patch` (no `call`)
- Exactly 2 blank lines between MockGlucoseReading and TestDexcomReadings
- `with`-continuation lines in all affected blocks use 14-space indent
- No `patch.dict(os.environ, ...)` wrapper in the missing-username or missing-password tests
</success_criteria>

<output>
After completion, create `.planning/quick/260424-u1l-fix-dexcom-readings-test-py-google-pytho/260424-u1l-SUMMARY.md`
</output>
